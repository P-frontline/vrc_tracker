# vrc_tracker
*for study*
Need to install - backend(node.js,axios(call http),VRChat API)
                - frontend(Electron + React)
                - DB(SQLite, Prisma(ORM) or SQL
Need to know - VRChat Auth login, JSON parsing etc...
