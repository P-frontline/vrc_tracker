# vrc_tracker <br/>
*for study* <br/>
What I used<br/>
Backend (node.js, axios(call http), VRChat API)<br/>
Frontend (Electron + React)<br/>
DB (SQLite, Prisma(ORM) or SQL<br/><br/>

Need to know - VRChat Auth login, JSON parsing etc...<br/>
